<div class="panel panel-default">
  <div class="panel-body">

    <%= form_for(@rating_reason, html: { class: 'form-horizontal', role: 'form' }) do |f| %>
      <% if @rating_reason.errors.any? %>
        <div class="alert alert-danger alert-dismissable" role="alert">
          <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
          <h4><%= pluralize(@rating_reason.errors.count, 'error') %> prohibited this rating_reason from being saved:</h4>

          <ul>
          <% @rating_reason.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <%= f.text_field :description, class: 'form-control' %>
      <%= f.text_field :rating_type, class: 'form-control' %>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <%= f.submit class: 'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
</div>