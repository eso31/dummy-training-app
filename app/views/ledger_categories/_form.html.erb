<div class="panel panel-default">
  <div class="panel-body">
    <%= form_for(@ledger_category, html: {class: 'form-horizontal', role: 'form' }) do |f| %>
      <% if @ledger_category.errors.any? %>
        <div class="alert alert-danger alert-dismissable" role="alert">
          <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
          <h4><%= pluralize(@ledger_category.errors.count, 'error') %> prohibited this ledger_category from being saved:</h4>

          <ul>
            <% @ledger_category.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= f.text_field :name, class: 'form-control' %>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <%= f.submit class: 'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
</div>